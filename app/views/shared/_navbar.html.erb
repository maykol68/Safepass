<nav class="bg-white border-b border-gray-200 shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 items-center justify-between">
      <div class="flex items-center space-x-8">
        <%= logo(size: 'text-xl', extra_classes: 'hover:text-indigo-600
        transition') %>

        <div class="hidden md:flex space-x-6">
          <%= link_to 'About', about_path, class: "text-gray-600 font-medium
          hover:text-indigo-600 transition-colors duration-200" %>
        </div>
      </div>

      <!-- Auth links -->
      <div class="hidden md:flex items-center space-x-4">
        <% if user_signed_in? %>
        <div class="relative group inline-block">
          <button class="bg-blue-900 text-white px-4 py-2 rounded-md">
            Opciones
          </button>

          <div
            class="absolute right-0 mt-2 w-44 bg-white rounded-md shadow-lg hidden group-hover:block"
          >
            <h6>
              Signed in as:<br> <%= current_user.email %>
            </h6>
            <%= link_to 'Config', edit_user_registration_path, class:"block px-4 py-2 hover:bg-gray-100"%>
            <%= link_to 'Sign Out', destroy_user_session_path, data: {
            turbo_method: :delete }, class: "block px-4 py-2 hover:bg-gray-100"%>
          </div>
        </div>

        <% else %> <%= link_to 'Login', user_session_path, class: "text-gray-600
        font-medium hover:text-indigo-600 transition-colors duration-200" %> <%=
        link_to 'Sign up', new_user_registration_path, class: "bg-indigo-600
        text-white px-4 py-2 rounded-lg font-medium hover:bg-indigo-700
        transition-colors duration-200" %> <% end %>
      </div>
    </div>
  </div>
</nav>
